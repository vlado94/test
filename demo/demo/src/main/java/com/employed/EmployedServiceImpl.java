package com.employed;
/*package app.employed;

import java.util.List;

import javax.transaction.Transactional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.google.common.collect.Lists;

@Service
@Transactional
public class EmployedServiceImpl implements EmployedService{
	
	private final EmployedRepository repository;
	
	@Autowired
	public EmployedServiceImpl(final EmployedRepository repository){
		this.repository = repository;
	}
	@Override
	public List<Employed> findAll() {
		return Lists.newArrayList(repository.findAll());
	}

	@Override
	public Employed save(Employed employed) {
		return repository.save(employed);
	}

	@Override
	public Employed findOne(Long id) {
		return repository.findOne(id);
	}

	@Override
	public void delete(Long id) {
		repository.delete(id);
	}

}
*/